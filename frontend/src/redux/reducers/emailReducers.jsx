const initialState = {
    emailsSent: 0,
    errors: null,
  };
  
  const emailReducer = (state = initialState, action) => {
    switch (action.type) {
      // Handle actions
      default:
        return state;
    }
  };
  
  export default emailReducer;
  